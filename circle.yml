machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t fankserver.com/fankserver/website:$CIRCLE_SHA1 .

deployment:
  hub:
    branch: master
    commands:
      - docker push fankserver.com/fankserver/website
