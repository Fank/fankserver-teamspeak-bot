machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t registry.fankserver.com/fankserver/website:$CIRCLE_SHA1 .

test:
  override:
    - echo "Temporarily disabled"

deployment:
  hub:
    branch: master
    commands:
      - docker push registry.fankserver.com/fankserver/website
